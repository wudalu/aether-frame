# Payloads

This folder is reserved for structured per-call inputs such as:

1. `planning_brief`
2. `current_step`
3. `review_package`

These objects are dynamic task data. They should not be hard-coded into prompt fragment files.
