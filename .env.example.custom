# Example Environment Configuration for Custom ID Generation
# Copy this to .env.custom to test custom ID prefixes

# Custom User Configuration
DEFAULT_USER_ID=test_user
DEFAULT_APP_NAME=my_aether_app

# Custom ID Prefixes
RUNNER_ID_PREFIX=custom_runner
SESSION_ID_PREFIX=custom_session
AGENT_ID_PREFIX=custom_agent
DOMAIN_AGENT_ID_PREFIX=my_domain_agent

# Custom Model Defaults
DEFAULT_ADK_MODEL=gemini-1.5-pro
DEFAULT_AUTOGEN_MODEL=gpt-4-turbo
DEFAULT_LANGGRAPH_MODEL=claude-3-5-sonnet-20241022

# Agent Type Override
DEFAULT_AGENT_TYPE=custom_adk_domain_agent

# Example of how to test:
# 1. Copy this file to .env.custom
# 2. Run: ENV_FILE=.env.custom python tests/e2e/test_complete_aiassistant_flow.py
# 3. Observe custom ID prefixes in the output